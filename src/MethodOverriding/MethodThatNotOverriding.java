/*
1.A method declared final cannot be overridden.

2. A method declared static cannot be overridden but can be re-declared.

3. If a method cannot be inherited, then it cannot be overridden.

4. Can't override the private method

5.A subclass within the same package as the instance's superclass can override any superclass
method that is not declared private or final.

Note : Synchronized and strictFp method , we can override this method
 */

package MethodOverriding;

public class MethodThatNotOverriding {
}
